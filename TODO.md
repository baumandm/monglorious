# TODO

## Expected Syntax:

    "show dbs"
    "show collections"
    "show users"
    "show roles"
    "show profile"
    "show databases"
    "db.<collection>.find(<query>)"
    "db.<collection>.findOne(<query>)"
    "db.<collection>.count()"
    "db.<collection>.find(<query>).count"
